<?xml version="1.0" encoding="utf-8"?>
<!-- The window the system shows before Flutter has drawn anything. Painted with
     the app's own background so the handoff to the first frame is invisible. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/shonenx_bg" />
</layer-list>
